// Re-export from shared location — DomainPage is used by both (default)/ and admin/ routes
export {
  DomainPageHOP,
  type DomainPage,
  type DomainPageCombinedProps,
  type DomainParams,
  type DomainProps,
} from "@/lib/DomainPage";
